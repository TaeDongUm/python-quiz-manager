공부한 내용 기록

# 백업이란?

- **`score_history`를 `[]`로 지웠는데 6번에서 이전 기록이 보인 현상은 백업 때문이라기보다 메모리 때문** 
- 프로그램 시작 시 `QuizGame.__init__()`에서 `load_state()`를 딱 한 번 실행하고 그때 JSON의 `score_history`가 `ScoreManager` 안의 리스트로 올라옵니다. 이후 6번은 JSON 파일을 다시 읽는 게 아니라 그 메모리의 리스트를 출력합니다. 

```text
프로그램 실행
    ↓
state.json → 메모리
[기존 기록 1개]
    ↓
실행 중 state.json을 직접 수정
score_history: []
    ↓
6번 선택
    ↓
파일을 다시 읽는 게 아님
    ↓
메모리의 [기존 기록 1개] 출력
```

이 상태에서 `Ctrl+C`를 누르면 `save_state()`가 실행되기 때문에 **메모리에 남아 있던 옛 기록을 다시 `state.json`에 써버릴 수도 있습니다.** 

검증하려면 프로그램을 완전히 종료한 다음 `state.json`을

```json
"score_history": []
```

- **백업은 매번 프로그램을 실행할 때가 아니라, 값을 저장하기 직전에 만드는 게 좋습니다.** 현재 `save()`는 먼저 `_create_backup()`을 호출하고 그 뒤 새로운 `state.json`을 씁니다. 

```text
현재 정상 state.json
        ↓
state.json.bak으로 복사
        ↓
새로운 데이터를 state.json에 저장
```

예를 들어:

```text
state.json     = 상태 B
state.json.bak = 상태 A

새 상태 C 저장

↓ 백업

state.json.bak = 상태 B

↓ 저장

state.json     = 상태 C
```

**백업과 현재 파일이 일부러 한 단계 차이나는 것이 좋습니다.**

프로그램을 켤 때마다 백업한다면 데이터가 바뀌지도 않았는데 파일 복사를 계속 수행하게 되고, 복구용 백업을 만들 이유도 없습니다. 작은 JSON에서는 성능 차이가 사실상 의미 없지만 개념적으로도 **“변경 직전 정상 상태를 보관한다”**가 백업 목적에 더 정확합니다.

다만 현재 `load()`에는 한 가지 정책이 있습니다. `state.json`이 **손상됐으면** `.bak`을 복원하지만, `state.json` 자체가 **없으면** 백업을 확인하지 않고 바로 기본 퀴즈로 시작합니다. 

- **`state.json.bak` 일부를 지우거나 과거 값으로 만들어도 정상 `state.json`이 있다면 문제없습니다.** 프로그램은 정상 상황에서는 `.bak`을 읽지 않습니다. `state.json`이 손상돼서 정상적으로 읽을 수 없을 때만 `_restore_backup()`을 호출합니다. 

예를 들어:

```text
state.json     = 최신 상태 C
state.json.bak = 옛 상태 A
```

여도 프로그램은 C를 사용합니다.

그리고 다음 저장이 일어나면:

```text
현재 C를 백업
↓
state.json.bak = C

새 상태 D 저장
↓
state.json = D
```

가 됩니다.

오히려 **`state.json`과 `.bak`을 항상 똑같이 유지하면 백업의 의미가 없습니다.**

만약:

```text
state.json     → 손상
state.json.bak → 손상
```

이면 둘 다 사용할 수 없기 때문에 현재 코드는 기본 데이터로 초기화합니다. 이것도 올바른 fallback입니다. 

그리고 `.bak`은 현재 GitHub 저장소에는 올라가 있지 않은 로컬 파일입니다. 그런데 `.gitignore`에도 아직 `state.json.bak`이 등록되어 있지 않습니다. 실수로 `git add .`을 하면 들어갈 수 있으므로 저는 다음 한 줄을 추가하는 것을 추천합니다. 

```gitignore
state.json.bak
```

6. **`best_score` 초기값으로 `-100`을 쓰는 것보다 현재 코드가 이미 지원하는 `None`을 쓰는 게 가장 좋습니다.** 실제 `ScoreManager`도 `best_score: Optional[int] = None`으로 만들어져 있고, `show_score()`도 `None`이면 `"아직 퀴즈를 풀지 않았습니다."`라고 처리합니다. 즉 설계상 이미 정답이 `None`입니다. 

JSON에서는 Python의 `None`이:

```json
null
```

입니다.

따라서 정말 초기 상태라면:

```json
{
    "quizzes": [...],
    "best_score": null,
    "score_history": []
}
```

가 가장 자연스럽습니다.

`0`은 실제로 퀴즈를 다 틀린 사용자가 받을 수 있는 **유효한 점수**이고, `100`도 유효한 점수입니다. `-100`은 구분은 가능하지만 실제 점수 범위를 벗어난 임의의 특수값을 만든 것이므로 `None`보다 의미가 불명확합니다.

다만 한 가지는 구분해야 합니다. **`score_history`가 비었다고 무조건 `best_score`도 `None`이어야 하는 것은 아닙니다.** 최고점수 기능은 기본 요구사항이고 히스토리는 보너스 기능이라 논리적으로 독립시킬 수 있습니다. 단, `"아직 단 한 번도 플레이하지 않은 초기 상태"`를 표현하려는 거라면 `null + []`가 가장 정확합니다.



> **백업 파일은 현재 파일과 항상 동기화하는 복사본이 아니라, 새로운 상태를 저장하기 직전의 마지막 정상 상태를 보관하는 복구 지점으로 사용했습니다.**
